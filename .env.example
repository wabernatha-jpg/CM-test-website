# Copy to .env for local use. Names only, no values. Production values live in Vercel env vars.
# Master key registry: EA/.env (note: nothing for this repo is stored there yet).

# Contact form email (Resend)
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=

# Optional: path to a local Chrome for Puppeteer screenshots
PUPPETEER_EXECUTABLE_PATH=
